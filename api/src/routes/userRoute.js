const {Router} = require('express');
const {} = require('../controllers/userController');

const router = Router();

router.get('/', );
router.post('/', );
router.put('/:id', );
router.delete('/:id', );

module.exports = router;