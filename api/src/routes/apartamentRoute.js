const {Router} = require('express');
const {} = require('../controllers/apartamentController');

const router = Router();

router.get('/', );
router.post('/', );
router.put('/:id', );
router.delete('/:id', );

module.exports = router;