

const initialState = {
    apartments:[]
}



function apartmentReducer(state = initialState, action){
    switch (action.type) {
        case '':
            
            return {...state};
    
        default:
            return {...state};
    }
}


export default apartmentReducer